class TestBuild {
    constructor(headline) {
        this.headline = headline
    }

    setHeadline(val) {
        this.headline = val
    }

    getHeadline() {
        return this.headline
    }
}

export default{
    TestBuild
}